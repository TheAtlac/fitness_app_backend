{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "docker-compose up dev",
            "type": "shell",
            "command": "docker compose -f docker-compose.yaml -f docker-compose.dev.yaml up --build -d"
        },
        {
            "label": "docker-compose down dev",
            "type": "shell",
            "command": "docker compose -f docker-compose.yaml -f docker-compose.dev.yaml down",
            "problemMatcher": []
        },
        {
            "label": "docker-compose down dev (with volumes)",
            "type": "shell",
            "command": "docker compose -f docker-compose.yaml -f docker-compose.dev.yaml down --volumes"
        },
        {
            "label": "docker-compose up debug",
            "type": "shell",
            "command": "docker compose -f docker-compose.yaml -f docker-compose.debug.yaml up --build -d"
        },
        {
            "label": "docker-compose down debug",
            "type": "shell",
            "command": "docker compose -f docker-compose.yaml -f docker-compose.debug.yaml down",
            "problemMatcher": []
        },
        {
            "label": "docker-compose down debug (with volumes)",
            "type": "shell",
            "command": "docker compose -f docker-compose.yaml -f docker-compose.debug.yaml down --volumes"
        }
    ]
}